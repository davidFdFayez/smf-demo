# When unset, Vite's dev server proxies /api → http://localhost:5080 (see vite.config.ts).
# Set this to bypass the proxy and hit the API directly, e.g. in production builds.
# VITE_API_BASE_URL=http://localhost:5080
